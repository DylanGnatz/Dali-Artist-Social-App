<!DOCTYPE html>
<html>
  <head>
    <title>Dali</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex flex-col">
      <div class="w-full mx-auto align-middle">
        <% if notice %>
          <p class="w-full mx-auto align-middle bg-green-400"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="w-full mx-auto align-middle bg-red-400"><%= alert %></p>
        <% end %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
